#!/bin/sh -xe
s=/tmp/tweemux
if [ ! -e $s ]; then # I'm the leader
    tmux -S $s start-server
    chmod 777 $s
    tmux -S $s "$@"
    rm $s
else # I'm the follower
    if [ $# -gt 0 ]; then
        tmux -S $s "$@"
    else
        tmux -S $s attach
    fi
fi
