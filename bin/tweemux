#!/bin/sh -xe
s=/tmp/tweemux.sock
PATH=/usr/local/bin:$PATH
sessmux() { tmux -S $s "$@"; }
if [ ! -e $s ]; then
    sessmux start-server
    chmod 777 $s
fi

if [ $# -gt 0 ]; then
    sessmux "$@"
else
    sessmux attach || sessmux new-session
fi
