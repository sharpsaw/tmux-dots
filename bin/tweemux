#!/bin/sh -xe
s=/tmp/tweemux
sessmux() { tmux -S $s "$@"; }
if [ ! -e $s ]; then
    sessmux start-server
    chmod 777 $s
fi

if [ $# -gt 0 ]; then
    sessmux "$@"
else
    sessmux attach || sessmux new-session
fi
